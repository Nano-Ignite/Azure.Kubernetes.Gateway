apiVersion: alb.networking.azure.io/v1
kind: FrontendTLSPolicy
metadata:
  name: %APP_NAME%-tls-policy
  namespace: %KUBERNETES_NAMESPACE%
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: %APP_NAME%
    namespace: %KUBERNETES_NAMESPACE%
  default:
    policyType:
      name: 2023-06-S
      type: predefined
